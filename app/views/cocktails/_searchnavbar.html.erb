<div class="searchbar flex mx-20px pb-10px bb-cm">
  <i class="fas fa-search"></i>
  <%= form_tag search_path, method: :get do %>
    <%= text_field_tag :query, params[:query], class: "border-0 width-70vw focus-outline-none", placeholder: "Looking for something more specific?" %>
  <% end %>
</div>
